on:
  push:
    branches:
      - main
name: semantic-release
jobs:
  make-release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-node@v1
        with:
          node-version: "12.x"
      - run: echo something

